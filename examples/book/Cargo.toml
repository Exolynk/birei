[package]
name = "birei-book"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
birei = { path = "../..", features = ["embedded"] }
leptos = { version = "0.7", features = ["csr"] }
leptos_router = { version = "0.7" }
web-sys = { version = "0.3", features = ["HtmlInputElement"] }
